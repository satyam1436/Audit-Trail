class RecordCustomsInspectionCommand {
    constructor({ containerId, inspector }) {
        this.containerId = containerId;
        this.inspector = inspector;
    }
}

export default RecordCustomsInspectionCommand;